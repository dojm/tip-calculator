const billAmount = document.getElementById('bill-amount');
let tipPercentage = document.getElementById('tip-percentage');

const calculateTip = (e) => {
  

  e.preventDefault(); 
}

document.getElementById('calculator').addEventListener('submit', calculateTip);